Smallest valid registry: only the two required top-level fields (`toolset_name`, `version`) plus one well-formed tool.
Confirms the adapter accepts a registry when all required fields are present and all values are schema-conformant.
Spec: `spec/02-toolsets-schema.md` §"Top-level fields", `spec/05-conformance.md` §L1.
