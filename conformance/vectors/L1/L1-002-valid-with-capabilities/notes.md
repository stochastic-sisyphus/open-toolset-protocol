Registry with all four capability flags set to `true`. Validates that the `capabilities` object is accepted when all known properties are present.
Verifies the adapter does not reject capabilities it recognises, and that boolean defaults are overridable.
Spec: `spec/02-toolsets-schema.md` §"Capabilities object", `schemas/toolsets.schema.json` `$defs.capabilities`.
