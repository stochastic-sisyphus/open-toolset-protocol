Same registry shape as L2-007 but `cymbal` is invoked before the phase transition to `surgery`. Required-tool check passes.
Confirms the positive path: transition is permitted once all required tools for the exiting phase have been invoked at least once.
Spec: `spec/03-adapter.md` §"Required tool enforcement", §"Phase gating algorithm".
